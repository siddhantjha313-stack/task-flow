{
  "name": "taskflow-ai",
  "version": "1.0.0",
  "private": true,
  "description": "TaskFlow AI - a premium full-stack project and task management SaaS platform.",
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n API,WEB -c cyan,magenta \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server",
    "seed": "npm run seed --workspace server",
    "lint": "npm run lint --workspace client",
    "check": "npm run lint --workspace client && npm run build"
  },
  "engines": {
    "node": ">=20.11.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
